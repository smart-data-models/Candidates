{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$schemaVersion": "0.0.1",
  "$id": "https://raw.githubusercontent.com/smart-data-models/Candidates/master/standards/edint-infraestructura/models/UnplannedIncidence/schema.json",
  "modelTags": "EDINT,UrbanInfrastructure,Traffic",
  "title": "Smart Data Models - EDINT Infrastructure - UnplannedIncidence",
  "description": "Unplanned traffic incidence in a city (edinttrafico:UnplannedIncidence, subclass of edinttrafico:Incidence and geosparql:Feature). The source ontology defines no properties of its own beyond those inherited from Incidence (incidenceInSection, incidenceType, startDate, endDate, incidenceAttachment, numDirections). Formerly excluded as part of an external-ontology traffic branch; now natively defined as of source ontology v0.2.1.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uri",
      "description": "Property. Unique identifier of the entity"
    },
    "type": {
      "type": "string",
      "enum": [
        "UnplannedIncidence"
      ],
      "description": "Property. NGSI-LD Entity type. It must be equal to 'UnplannedIncidence'"
    },
    "dateCreated": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Entity creation timestamp. This will usually be allocated by the storage platform."
    },
    "dateModified": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Timestamp of the last modification of the entity. This will usually be allocated by the storage platform."
    },
    "name": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/name'. Name of this item."
    },
    "alternateName": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/alternateName'. An alternative name for this item."
    },
    "description": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/description'. Description of this item."
    },
    "source": {
      "type": "string",
      "description": "Property. A sequence of characters giving the source of the entity data."
    },
    "dataProvider": {
      "type": "string",
      "description": "Property. A sequence of characters identifying the provider of the harmonised data entity."
    },
    "seeAlso": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Property. Model:'https://schema.org/URL'. list of uri pointing to additional resources about the item."
    },
    "location": {
      "type": "object",
      "description": "GeoProperty. Geojson reference to the item. Point providing the geographical position of the entity.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "Point"
          ]
        },
        "coordinates": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "number"
          },
          "description": "Property. Coordinates of the Point, in WGS84 [longitude, latitude] order."
        }
      },
      "required": [
        "type",
        "coordinates"
      ]
    },
    "incidenceInSection": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. The StreetSection where the incidence takes place (edinttrafico:incidenceInSection)."
    },
    "incidenceType": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. Type of incidence. The source ontology does not publish a controlled vocabulary for this yet (unlike e.g. Facility.infrastructureType); kept as a generic Relationship."
    },
    "startDate": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. The start date and time of the incidence (edinttrafico:startDate, schema:startDate)."
    },
    "endDate": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. The end date and time of the incidence (edinttrafico:endDate, schema:endDate)."
    },
    "incidenceAttachment": {
      "type": "string",
      "format": "uri",
      "description": "Property. A document where more information about the incidence can be found (edinttrafico:incidenceAttachment)."
    },
    "numDirections": {
      "type": "integer",
      "description": "Property. Number of directions of traffic affected (edinttrafico:numDirections)."
    }
  },
  "required": [
    "id",
    "type"
  ]
}
