{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$schemaVersion": "0.0.1",
  "$id": "https://raw.githubusercontent.com/smart-data-models/Candidates/master/standards/simple-federated-businessservice/models/CargoHandlingService/schema.json",
  "modelTags": "FEDeRATED,SIMPLE,Logistics,BusinessService",
  "title": "Smart Data Models - SIMPLE Federated Business Service - CargoHandlingService",
  "description": "A service for the handling of cargo, e.g. moving cargo from a truck to a train, or from a truck to a deposit in a freight terminal (simple-bs:CargoHandlingService, SIMPLE extension to the FEDeRATED Business Service ontology, subclass of LogisticsService).",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uri",
      "description": "Property. Unique identifier of the entity"
    },
    "type": {
      "type": "string",
      "enum": [
        "CargoHandlingService"
      ],
      "description": "Property. NGSI-LD Entity type. It must be equal to 'CargoHandlingService'"
    },
    "dateCreated": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Entity creation timestamp."
    },
    "dateModified": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Timestamp of the last modification."
    },
    "name": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/name'. Human-readable label (rdfs:label in the source)."
    },
    "source": {
      "type": "string",
      "description": "Property. A sequence of characters giving the source of the entity data."
    },
    "dataProvider": {
      "type": "string",
      "description": "Property. A sequence of characters identifying the provider of the harmonised data entity."
    },
    "serviceName": {
      "type": "string",
      "description": "Property. businessService:serviceName (inherited from LogisticsService). Each type of logistics service can be described using a name."
    },
    "hasLocation": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. businessService:hasLocation (inherited from LogisticsService). Location of the logistics service. Range: schema:Place (out of scope of this candidate)."
    },
    "usedTransportMeans": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Relationship. businessService:usedTransportMeans (inherited from LogisticsService). Transport means involved with the logistics service. Range: DigitalTwin:TransportMeans (out of scope of this candidate)."
    },
    "usedPackagedProducts": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Relationship. businessService:usedPackagedProducts (inherited from LogisticsService). Packaged products that are part of the logistics service. Range unspecified in the source ontology; modeled generically as a URI relationship."
    }
  },
  "required": [
    "id",
    "type"
  ]
}
