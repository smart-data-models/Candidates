{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$schemaVersion": "0.0.1",
  "$id": "https://raw.githubusercontent.com/smart-data-models/Candidates/master/standards/edint-trafico/models/PlannedIncident/schema.json",
  "modelTags": "EDINT,Transportation",
  "title": "Smart Data Models - EDINT Traffic - PlannedIncident",
  "description": "Planned traffic incident, can be recurrent or not (edinttrafico:PlannedIncident, subclass of edinttrafico:Incident and geosparql:Feature). Bare 'Incident' is deliberately not materialized as its own entity, matching this candidate family's precedent for abstract SOSA-style bases (e.g. Sensor). Renamed from 'Incidence'/'PlannedIncidence' used in edint-infraestructura's own bundled v0.2.1 copy of this pattern -- see standard-metadata.yaml.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uri",
      "description": "Property. Unique identifier of the entity"
    },
    "type": {
      "type": "string",
      "enum": [
        "PlannedIncident"
      ],
      "description": "Property. NGSI-LD Entity type. It must be equal to 'PlannedIncident'"
    },
    "dateCreated": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Entity creation timestamp. This will usually be allocated by the storage platform."
    },
    "dateModified": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Timestamp of the last modification of the entity. This will usually be allocated by the storage platform."
    },
    "name": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/name'. Name of this item."
    },
    "alternateName": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/alternateName'. An alternative name for this item."
    },
    "description": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/description'. Description of this item."
    },
    "source": {
      "type": "string",
      "description": "Property. A sequence of characters giving the source of the entity data."
    },
    "dataProvider": {
      "type": "string",
      "description": "Property. A sequence of characters identifying the provider of the harmonised data entity."
    },
    "seeAlso": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Property. Model:'https://schema.org/URL'. list of uri pointing to additional resources about the item."
    },
    "location": {
      "type": "object",
      "description": "GeoProperty. Geojson reference to the item. Point providing the geographical position of the entity.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "Point"
          ]
        },
        "coordinates": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "number"
          },
          "description": "Property. Coordinates of the Point, in WGS84 [longitude, latitude] order."
        }
      },
      "required": [
        "type",
        "coordinates"
      ]
    },
    "incidentInSection": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. The StreetSection where the incident takes place (edinttrafico:incidentInSection)."
    },
    "incidentType": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. Type of incident. The source ontology does not publish a controlled vocabulary for this yet; kept as a generic Relationship."
    },
    "startDate": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. The (expected) start date and time of the incident (schema:startDate). No formal rdfs:domain declared in the source; included per the property's own comment ('of an incident')."
    },
    "endDate": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. The end date and time of the incident (schema:endDate). Same no-formal-domain situation as startDate."
    },
    "incidentAttachment": {
      "type": "string",
      "format": "uri",
      "description": "Property. A document where more information about the incident can be found (edinttrafico:incidentAttachment)."
    },
    "numDirections": {
      "type": "integer",
      "description": "Property. Number of directions of traffic affected (edinttrafico:numDirections)."
    },
    "expectedEndDate": {
      "type": "string",
      "format": "date-time",
      "description": "Property. The expected end date and time of a planned incident (edinttrafico:expectedEndDate)."
    },
    "isRecurrent": {
      "type": "boolean",
      "description": "Property. Whether the planned incident is recurrent or not (edinttrafico:isRecurrent)."
    },
    "recurrence": {
      "type": "string",
      "description": "Property. Description of the recurrence pattern (edinttrafico:recurrence). The source ontology does not constrain the format."
    }
  },
  "required": [
    "id",
    "type"
  ]
}
