{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$schemaVersion": "0.1.0",
  "$id": "https://raw.githubusercontent.com/smart-data-models/Candidates/master/standards/edint-infraestructura/models/Violation/schema.json",
  "modelTags": "EDINT,UrbanInfrastructure",
  "title": "Smart Data Models - EDINT Infrastructure - Violation",
  "description": "Non-compliance or violation of traffic or access rules, road laws or local ordinances by a person or a driver or owner of a vehicle (edintinf:Violation).",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uri",
      "description": "Property. Unique identifier of the entity"
    },
    "type": {
      "type": "string",
      "enum": [
        "Violation"
      ],
      "description": "Property. NGSI-LD Entity type. It must be equal to 'Violation'"
    },
    "dateCreated": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Entity creation timestamp. This will usually be allocated by the storage platform."
    },
    "dateModified": {
      "type": "string",
      "format": "date-time",
      "description": "Property. Model:'https://schema.org/DateTime'. Timestamp of the last modification of the entity. This will usually be allocated by the storage platform."
    },
    "name": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/name'. Name of this item."
    },
    "alternateName": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/alternateName'. An alternative name for this item."
    },
    "description": {
      "type": "string",
      "description": "Property. Model:'https://schema.org/description'. Description of this item."
    },
    "source": {
      "type": "string",
      "description": "Property. A sequence of characters giving the source of the entity data."
    },
    "dataProvider": {
      "type": "string",
      "description": "Property. A sequence of characters identifying the provider of the harmonised data entity."
    },
    "seeAlso": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Property. Model:'https://schema.org/URL'. list of uri pointing to additional resources about the item."
    },
    "complaintCancelled": {
      "type": "boolean",
      "description": "Property. Whether the complaint associated with this violation has been cancelled/annulled."
    },
    "violationType": {
      "type": "string",
      "enum": [
        "Minor",
        "Severe",
        "VerySevere",
        "NonAuthorizedAccessToLowEmissionsZone",
        "NonAuthorizedAccessToPedestrianZone"
      ],
      "description": "Property. The type of violation, from the ViolationType controlled vocabulary."
    },
    "vehicle": {
      "type": "string",
      "format": "uri",
      "description": "Relationship. The vehicle that committed the violation (edintinf:linkedTo). 'Vehicle' is referenced but not defined by the source ontology."
    }
  },
  "required": [
    "id",
    "type"
  ]
}
